<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 必要なパーミッション -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- HTTP 通信 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 高度なダウンロードマネージャー -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 外部ストレージの読み取り -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 画像・音楽などのメディア読み取り -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 外部ストレージへの書き込み -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- 音声録音 -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- カメラ -->
    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER" /> <!-- ダウンロードマネージャー -->
    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER_ADVANCED" /> <!-- 画像・音楽などのメディア書き込み -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" /> <!-- ストレージのマウント解除（必要に応じて） -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- ネットワーク状態の監視 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- Wi-Fi 接続状態の監視 -->
    <!-- TV向けの特定の機能を要求 -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LightWeb20"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".DesktopWebViewActivity"
            android:exported="false" />
        <activity
            android:name=".TabActivity"
            android:exported="false" /> <!-- HTTP通信を許可 -->
        <activity
            android:name=".LicenseActivity"
            android:exported="false" />
        <activity
            android:name=".CreditActivity"
            android:exported="false" />
        <activity
            android:name=".drawerLayout"
            android:exported="false" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>